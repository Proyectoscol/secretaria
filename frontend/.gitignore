# Next.js build output
.next/
out/

# Dependencies (already covered by root, repeated for clarity)
node_modules/

# Override root rules — these files MUST be committed for Docker builds.
# The root .gitignore (OpenClaw monorepo) blocks both; force-tracked with git add -f.
!package-lock.json
!.gitignore
