[pytest]
testpaths = tests/kos
asyncio_mode = auto
markers =
    integration: marks tests that require a live PostgreSQL + pgvector instance
    slow: marks tests that take >10s (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning:pydantic
    ignore::DeprecationWarning:sentence_transformers
